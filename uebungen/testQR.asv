clear all;
format compact;
fprintf('----------------------------------------\n');


A = [4, 1, -2, 2;
   1 & 2 &  0 &  1;
  -2 & 0 &  3 & -2 \\
   2 & 1 & -2 & -1
];

y = A(:,1);
v = y
v = v - sign(y(1))*(norm(y)*ones(length(v),1))

Q = ones(length(v)) - 2 * (v*v') / norm(v)

Q*A

Q*y